"use client";
import { useMemo, useState } from "react";
import { AnimatePresence, motion } from "framer-motion";
import { Archive, Bell, Check, CheckCheck, ChevronDown, Info, Menu, MessageCircle, MoreHorizontal, Paperclip, Pin, Plus, Search, Send, Smile, Sun, Moon, Users, Video, Phone, X } from "lucide-react";
import { conversations, messages as messageMap, type Conversation, type Message } from "@/lib/chat-data";

function Avatar({ person, size="md" }: {person: Conversation; size?: "sm"|"md"|"lg"}) { return <div className={`relative grid shrink-0 place-items-center rounded-full font-semibold text-foreground ${size === "lg" ? "size-12 text-sm" : size === "sm" ? "size-9 text-xs" : "size-10 text-xs"}`} style={{backgroundColor:person.color}}>{person.initials}{person.online && <span className="absolute bottom-0 right-0 size-3 rounded-full border-2 border-card bg-primary" />}</div> }
function Status({status}:{status?:Message["status"]}) { return status ? <span className="ml-1 inline-flex align-middle text-primary">{status === "read" ? <CheckCheck className="size-3.5"/> : <Check className="size-3.5"/>}</span> : null }

export default function ChatApp() {
 const [activeId,setActiveId]=useState("maya"); const [query,setQuery]=useState(""); const [draft,setDraft]=useState(""); const [isDark,setIsDark]=useState(false); const [mobileOpen,setMobileOpen]=useState(false); const [sent,setSent]=useState<Message[]>([]);
 const active=conversations.find(c=>c.id===activeId) ?? conversations[0];
 const list=useMemo(()=>conversations.filter(c=>c.name.toLowerCase().includes(query.toLowerCase())),[query]);
 const thread=[...(messageMap[activeId] ?? []),...sent.filter(m=>activeId==="maya")];
 const send=()=>{if(!draft.trim()) return; setSent([...sent,{id:Date.now().toString(),text:draft.trim(),time:"Now",from:"me",status:"sent"}]);setDraft("")};
 const pick=(id:string)=>{setActiveId(id);setMobileOpen(false);setSent([])};
 return <div className={isDark?"dark":""}><main className="flex h-dvh overflow-hidden bg-background text-foreground"><aside className={`${mobileOpen?"translate-x-0":"-translate-x-full"} fixed inset-y-0 left-0 z-20 flex w-[88vw] max-w-[360px] flex-col border-r bg-card transition-transform lg:static lg:w-[360px] lg:translate-x-0`}><div className="flex items-center justify-between px-6 pb-5 pt-7"><div><p className="font-mono text-xs font-semibold uppercase tracking-[0.24em] text-primary">Yapp</p><h1 className="mt-1 text-2xl font-semibold tracking-tight">Messages</h1></div><div className="flex gap-1"><button aria-label="New message" className="rounded-full p-2.5 hover:bg-muted"><Plus className="size-5"/></button><button aria-label="Close menu" className="rounded-full p-2.5 hover:bg-muted lg:hidden" onClick={()=>setMobileOpen(false)}><X className="size-5"/></button></div></div><div className="px-5 pb-4"><div className="flex items-center gap-2 rounded-xl bg-muted px-3 py-2.5"><Search className="size-4 text-muted-foreground"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search conversations" className="min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-muted-foreground"/></div></div><div className="flex items-center gap-1 px-5 pb-3"><button className="rounded-full bg-accent px-3 py-1.5 text-xs font-medium text-primary">All</button><button className="rounded-full px-3 py-1.5 text-xs text-muted-foreground hover:bg-muted">Unread</button><button className="rounded-full px-3 py-1.5 text-xs text-muted-foreground hover:bg-muted">Groups</button></div><div className="flex flex-1 flex-col gap-1 overflow-y-auto px-3">{list.map(c=><button key={c.id} onClick={()=>pick(c.id)} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-left transition ${activeId===c.id?"bg-accent/70":"hover:bg-muted"}`}><Avatar person={c}/><span className="min-w-0 flex-1"><span className="flex items-center justify-between gap-2"><strong className="truncate text-sm">{c.name}</strong><small className="shrink-0 text-[11px] text-muted-foreground">{c.time}</small></span><span className="mt-1 flex items-center justify-between gap-2"><span className="truncate text-xs text-muted-foreground">{c.preview}</span>{c.unread&&<span className="grid size-5 shrink-0 place-items-center rounded-full bg-primary text-[10px] font-bold text-primary-foreground">{c.unread}</span>}</span></span></button>)}</div><div className="flex items-center justify-between border-t px-5 py-4"><div className="flex items-center gap-3"><div className="grid size-9 place-items-center rounded-full bg-primary text-xs font-semibold text-primary-foreground">AK</div><div><p className="text-sm font-medium">Alex Kim</p><p className="text-xs text-muted-foreground">Available</p></div></div><button aria-label="More options" className="rounded-full p-2 hover:bg-muted"><MoreHorizontal className="size-5"/></button></div></aside>{mobileOpen&&<button aria-label="Close sidebar" className="fixed inset-0 z-10 bg-black/20 lg:hidden" onClick={()=>setMobileOpen(false)}/>}<section className="flex min-w-0 flex-1 flex-col"><header className="flex items-center justify-between border-b bg-card px-4 py-4 sm:px-8"><div className="flex items-center gap-3"><button aria-label="Open conversations" className="rounded-full p-2 hover:bg-muted lg:hidden" onClick={()=>setMobileOpen(true)}><Menu className="size-5"/></button><Avatar person={active} size="lg"/><div><h2 className="font-semibold">{active.name}</h2><p className="text-xs text-muted-foreground">{active.online?"Online":"Last seen recently"}</p></div></div><div className="flex items-center gap-1"><button aria-label="Start video call" className="rounded-full p-2.5 hover:bg-muted"><Video className="size-5"/></button><button aria-label="Start audio call" className="rounded-full p-2.5 hover:bg-muted"><Phone className="size-5"/></button><button aria-label="Conversation info" className="rounded-full p-2.5 hover:bg-muted"><Info className="size-5"/></button></div></header><div className="flex flex-1 flex-col overflow-hidden bg-[radial-gradient(var(--border)_0.7px,transparent_0.7px)] [background-size:18px_18px]"><div className="flex items-center justify-center gap-2 py-5"><span className="rounded-full bg-card px-3 py-1 text-[11px] font-medium text-muted-foreground shadow-sm">Today</span></div><div className="flex flex-1 flex-col gap-3 overflow-y-auto px-4 pb-6 sm:px-[10%]">{thread.map((m,i)=><motion.div initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} transition={{delay:i*.04}} key={m.id+i} className={`flex ${m.from==="me"?"justify-end":"justify-start"}`}><div className={`max-w-[78%] rounded-2xl px-4 py-2.5 text-sm leading-6 shadow-sm ${m.from==="me"?"rounded-br-md bg-primary text-primary-foreground":"rounded-bl-md bg-card"}`}><p>{m.text}</p><p className={`mt-1 text-right text-[10px] ${m.from==="me"?"text-primary-foreground/70":"text-muted-foreground"}`}>{m.time}<Status status={m.status}/></p></div></motion.div>)}</div><div className="border-t bg-card/90 px-4 py-4 backdrop-blur sm:px-[8%]"><div className="flex items-end gap-2 rounded-2xl bg-muted p-2"><button aria-label="Attach file" className="rounded-xl p-2 text-muted-foreground hover:bg-card"><Paperclip className="size-5"/></button><textarea value={draft} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey&&!e.nativeEvent.isComposing&&e.keyCode!==229){e.preventDefault();send()}}} placeholder="Write a message..." rows={1} className="max-h-28 min-h-10 flex-1 resize-none bg-transparent px-1 py-2 text-sm outline-none placeholder:text-muted-foreground"/><button aria-label="Add emoji" className="rounded-xl p-2 text-muted-foreground hover:bg-card"><Smile className="size-5"/></button><button aria-label="Send message" onClick={send} className="grid size-10 shrink-0 place-items-center rounded-xl bg-primary text-primary-foreground transition hover:opacity-90"><Send className="size-4"/></button></div><p className="mt-2 hidden text-center text-[10px] text-muted-foreground sm:block">Press Enter to send · Shift + Enter for a new line</p></div></div></section><button onClick={()=>setIsDark(!isDark)} aria-label="Toggle theme" className="fixed bottom-5 right-5 grid size-10 place-items-center rounded-full border bg-card shadow-lg">{isDark?<Sun className="size-4"/>:<Moon className="size-4"/>}</button></main></div>
}
